# Самопроверка проекта (self-checklist)

Отметьте, что реализовано в вашем проекте.  
Этот чеклист помогает и вам, и преподавателю быстро понять, на каком уровне готовности находится проект.

В столбце «Да/Нет (студент)» поставьте `✅` или `❌`.  
В столбце «Где смотреть / комментарий» укажите файл(ы), где реализован соответствующий пункт.

> Важно: чеклист – это **самооценка**. Преподаватель может не согласиться с вашими отметками и скорректировать оценку.

---

## Таблица самопроверки

| #  | Критерий                                                                 | Да/Нет (студент) | Где смотреть / комментарий                          |
|----|---------------------------------------------------------------------------|------------------|-----------------------------------------------------|
| 1  | Сервис запускается по инструкциям из `project/README.md` и работает      | ✅ | `README.md` разделы «Локальный запуск» и «Docker»; `docker-compose.yml`; `Dockerfile` |
| 2  | Endpoint `/predict` использует **реальную модель**, а не заглушку        | ✅ | `src/tumor_app/infer.py` — загрузка 6 реальных `.pt` чекпоинтов; `src/tumor_app/api.py` |
| 3  | Есть EDA и хотя бы один эксперимент с метриками                          | ✅ | `notebooks/data_load.ipynb` — EDA и анализ данных; `notebooks/test.ipynb` — инференс с визуализацией; `artifacts/plots/` — кривые обучения |
| 4  | Есть baseline и улучшенная модель, есть **сравнение по метрикам**        | ✅ | `src/models/legacy_aip_v1.py` — baseline (SmallResNet + SimpleUNet); `src/models/classification.py`, `src/models/segmentation.py` — текущие модели; сравнение в `notebooks/test_compare.ipynb` и `report.md` §5 |
| 5  | Код не свален в один ноутбук: есть внятная структура в `src/`            | ✅ | `src/data/` — датасеты; `src/models/` — архитектуры; `src/training/` — циклы обучения; `src/tumor_app/` — API + CLI; `src/utils/` — чекпоинты, графики |
| 6  | Есть Dockerfile **или** понятный сценарий развёртывания без Docker       | ✅ | `Dockerfile` (python:3.12-slim, CPU-инференс); `docker-compose.yml` (volume для весов, порт 8000); инструкции в `README.md` |
| 7  | Есть `.env.example` и **нет** в репозитории реальных секретов/паролей    | ✅ | `configs/.env.example`; `.gitignore` исключает `.env`, `kaggle.json`, `*.pt`, `data/` |
| 8  | Реализованы логи/наблюдаемость (хотя бы консольные логи + `/health`)     | ✅ | `GET /health` → `{"status":"ok"}`; middleware логирует время каждого запроса; обучение пишет в `artifacts/logs/logsN.log` |
| 9  | В `report.md` **обоснован выбор финальной модели** по результатам экспериментов | ✅ | `report.md` §5.3 — таблица метрик v1 vs текущей, обоснование выбора SimpleCNNClassifier + UNetNoPadding |
| 10 | `project/README.md` и `report.md` позволяют понять сценарий демонстрации | ✅ | `README.md` — полные инструкции запуска (venv/Docker/CLI); `report.md` §9 — три сценария демонстрации |

---

## Подсчёт баллов и связь с оценкой

За каждый критерий, по которому вы честно можете поставить `✅`, засчитывается **1 балл**.  
Максимум – 10 баллов.

Преподаватель использует суммарное количество баллов **после проверки** в качестве ориентира для выставления оценки:

- Если проект **не проходит базовый минимум** (не запускается, отсутствует ключевой функционал, грубые нарушения, плагиат и т.п.) – оценка: **2** (проходной минимум не достигнут).
- Если минимум пройден, то ориентировочно:
  - **1-4 балла** → оценка **3** (проект принят, но на базовом уровне);
  - **5-8 баллов** → оценка **4** (хороший, рабочий проект);
  - **9-10 баллов** → оценка **5** (сильный, хорошо проработанный проект).

Окончательное решение по оценке остаётся за преподавателем и может учитывать:

- качество реализации внутри каждого пункта;
- дополнительные сильные стороны проекта (нестандартные решения, дополнительные функции, аккуратная архитектура и т.п.);
- соблюдение требований курса и дедлайнов.

---

**Итог самооценки: 10/10**
